<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Items: </h1>
    <ol>
        <% items.forEach(item => { %>
            <li>
                <% if (item.name !== 'undefined') { %>
                    <%= item.name %>
               <% } %>
            </li>
       <% }); %>
    </ol>
    <a href="/">Go back</a>
</body>
</html>